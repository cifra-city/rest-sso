type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ activate_email ]
      attributes:
        type: object
        required:
          - email
          - code
        properties:
          email:
            type: string
            description: "New email"
          code:
            type: string
            description: "User code"
