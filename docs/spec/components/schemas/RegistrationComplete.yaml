type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ registration_complete ]
      attributes:
        type: object
        required:
          - password
          - email
          - username
        properties:
          password:
            type: string
            description: "User password"
          email:
            type: string
            format: email
            description: "User email"
          username:
            type: string
            description: "User username"
